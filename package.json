{
  "name": "lavalamp",
  "version": "0.1.0",
  "description": "Cloudflare-native local AI coding harness",
  "bin": {
    "lavalamp": "./dist/server.mjs"
  },
  "type": "module",
  "scripts": {
    "build": "flue build --target node",
    "postinstall": "bun run build/postinstall.ts",
    "dev": "flue build --target node --watch"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.79.6",
    "@flue/cli": "^1.0.0-beta.1",
    "@flue/runtime": "^1.0.0-beta.2",
    "@oh-my-pi/hashline": "^16.0.6",
    "@opentui/core": "^0.4.1",
    "valibot": "^1.4.1"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
