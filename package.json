{
  "name": "lavalamp",
  "version": "0.1.0",
  "description": "Cloudflare-native local AI coding harness",
  "bin": {
    "lavalamp": "./bin/lavalamp"
  },
  "type": "module",
  "scripts": {
    "build": "flue build --target node && bun run build/patch-server.ts",
    "postinstall": "bun run build/postinstall.ts",
    "test": "bun test",
    "e2e": "bun run build && bun test --timeout=180000 ./testbench/e2e/actual-bin.e2e.ts",
    "typecheck": "tsc --noEmit",
    "dev": "flue build --target node --watch"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.79.6",
    "@flue/cli": "^1.0.0-beta.1",
    "@flue/runtime": "^1.0.0-beta.2",
    "@oh-my-pi/hashline": "^16.0.6",
    "@opentui/core": "^0.4.1",
    "valibot": "^1.4.1"
  },
  "devDependencies": {
    "@nigel-dev/opentui-test": "^0.1.0",
    "@opentui/react": "^0.4.3",
    "@types/bun": "^1.3.14",
    "@types/react": "^19.2.17",
    "react": "^19.2.7"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
