schema_version = "1.3"

[task]
name = "local/trial-score-aggregation"
description = "Repair benchmark score aggregation across all trial outcomes"
authors = [{ name = "Lavalamp" }]
keywords = ["typescript", "benchmark", "aggregation"]

[agent]
timeout_sec = 300.0

[verifier]
timeout_sec = 60.0

[environment]
network_mode = "no-network"
build_timeout_sec = 300.0
cpus = 1
memory_mb = 1024
storage_mb = 2048
