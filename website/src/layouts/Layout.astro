<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" type="image/png" href="/icon.png" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		
		<!-- SEO Meta Tags -->
		<title>lavalamp | Cloudflare-native local AI coding harness</title>
		<meta name="description" content="lavalamp is a local, terminal-based AI coding assistant built for Cloudflare. Run Workers AI models on your own account with zero-trust permissions, hash-anchored edits, and parallel subagents." />
		<meta name="keywords" content="lavalamp, cloudflare, AI coding, workers ai, local ai, coding assistant, opentui, hashline, local agent, developer tool" />
		<meta property="og:title" content="lavalamp | Cloudflare-native local AI coding harness" />
		<meta property="og:description" content="Sleek, secure, terminal-based AI coding companion. Runs Workers AI models on your own Cloudflare account with hash-anchored edits and zero-trust permissions." />
		<meta property="og:type" content="website" />
		
		<!-- Google Fonts -->
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Manrope:wght@400;500;600;700&family=Playwrite+US+Trad:wght@100..400&display=swap" rel="stylesheet">
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--color-accent: #FF5F1F;
		--color-blue: #6BA4FF;
		--color-border: #1e1e26;
		--color-border-light: #2a2a34;
		--color-cyan: #22D3EE;
		--color-dark-bg: #0c0c10;
		--color-dim: #4a4a56;
		--color-elevated: #131318;
		--color-focused-border: #FF5F1F;
		--color-gray: #7c7c8a;
		--color-green: #00BD7D;
		--color-input-bg: #111114;
		--color-input-focused: #16161a;
		--color-link: #C77DFF;
		--color-pink: #F472B6;
		--color-plan-accent: #2DD4BF;
		--color-red: #FF5B66;
		--color-white: #ffffff;
		--color-yellow: #EAB308;
		
		--font-sans: 'Manrope', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
		--font-serif: 'Fraunces', Georgia, serif;
		--font-mono: 'DM Mono', monospace;
		--font-script: 'Playwrite US Trad', cursive;
	}

	*, *::before, *::after {
		box-sizing: border-box;
	}

	html,
	body {
		margin: 0;
		padding: 0;
		width: 100%;
		min-height: 100%;
		background-color: var(--color-dark-bg);
		color: var(--color-white);
		font-family: var(--font-sans);
		overflow-x: hidden;
		line-height: 1.6;
		letter-spacing: normal;
	}

	/* Custom Scrollbar */
	::-webkit-scrollbar {
		width: 8px;
		height: 8px;
	}
	::-webkit-scrollbar-track {
		background: var(--color-dark-bg);
	}
	::-webkit-scrollbar-thumb {
		background: #2a2a34;
		border-radius: 4px;
	}
	::-webkit-scrollbar-thumb:hover {
		background: var(--color-accent);
	}

	::selection {
		background: rgba(255, 95, 31, 0.18);
		color: var(--color-white);
	}
</style>
